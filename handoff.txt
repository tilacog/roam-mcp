# Handoff: Add random_node MCP tool

## Task Summary
Add a new `random_node` MCP tool that returns a random node from the org-roam index.

## Current Status
✅ **Completed:**
1. Added `random_node` method to `RoamIndex` trait (`src/index/mod.rs`)
2. Implemented in both backends:
   - SQLite: `src/index/sqlite.rs` (uses `ORDER BY RANDOM() LIMIT 1`)
   - Scanner: `src/index/scan.rs` (uses `rand::prelude::IndexedRandom`)
3. Added tool params struct `RandomNodeParams` in `src/tools/query.rs`
4. Added tool function `random_node` in `src/tools/query.rs`
5. Registered tool in `RoamServer` in `src/server.rs`
6. Updated server instructions to list the new tool
7. Added `rand = "0.9"` dependency in `Cargo.toml`
8. Implemented `random_node` for test mocks in:
   - `src/tools/content.rs` (OneNode)
   - `src/tools/retrieval.rs` (FakeIndex)
   - `tests/stale_index.rs` (StaleIndex)
9. All existing tests pass (367 passed)

❌ **Remaining Issue:**
- CRAP metric gate failing for `SqliteIndex::random_node` (score 90, threshold 30) because the new function has 0% test coverage
- Minor syntax error in the test I added (double quotes in `text.contains("\"id\"")`)

## What Needs Fixing

### 1. Fix the test syntax error in `tests/mcp_integration.rs`
Line ~234 has:
```rust
text.contains("\"id\"") && text.contains("\"title\"")
```
Should be:
```rust
text.contains("\"id\"") && text.contains("\"title\"")
```
(Actually the issue was escaped quotes - use single quotes or proper escaping)

### 2. Add test coverage for `SqliteIndex::random_node` to pass CRAP gate
The function has cyclomatic complexity of 9 but 0% coverage. Options:
- Add unit test in `tests/sqlite_index.rs` that exercises the random_node function
- Or add integration test that uses the SQLite backend specifically

### 3. Run `just ci` to verify everything passes
After fixing the above, run:
```bash
cd /home/tilacog/.config/worktrunk/worktrees/roam-mcp/random-node
just ci
```

## Files Modified
- `src/index/mod.rs` - trait definition
- `src/index/sqlite.rs` - SQLite implementation
- `src/index/scan.rs` - Scanner implementation  
- `src/tools/query.rs` - tool params and function
- `src/server.rs` - tool registration and instructions
- `Cargo.toml` - rand dependency
- `src/tools/content.rs` - test mock
- `src/tools/retrieval.rs` - test mock
- `tests/stale_index.rs` - test mock
- `tests/mcp_integration.rs` - integration test (has syntax error)

## Quick Fix Commands
```bash
# Fix test syntax
sed -i 's/text.contains("\"id\"") && text.contains("\"title\"")/text.contains(\"\\\"id\\\"\") \&\& text.contains(\"\\\"title\\\"\")/' tests/mcp_integration.rs

# Add unit test for sqlite random_node (in tests/sqlite_index.rs)
# Then run:
cargo test
just ci
```